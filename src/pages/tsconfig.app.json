{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "jsx": "react-jsx", // Si tu utilises React, sinon adapte (ex. "preserve" pour d'autres frameworks)
    "baseUrl": "./",
    "paths": {
      "@/*": ["./src/*"] // Ajuste selon la structure de ton projet
    }
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}